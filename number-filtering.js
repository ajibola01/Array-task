const userScore = [  
{id: 1, name: 'Olarike', score: 34},  
{id: 2, name: 'Taiwo', score: 12},  
{id: 3, name: 'Kehinde', score: 27} ];
 //sort the array in descending order based on the user score 
 /***const sortByScore = userScore.map( p =>{ console.log(p. score)
  return p}).sort (function (p1, p2)
  { return p1.score - p2.score;});
  console.log (sortByScore);*/
  userScore.forEach(function (arrayScore){
      var x = arrayScore.prop1 + 2;
      console.log (x);
  });